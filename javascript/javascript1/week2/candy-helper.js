//Candy helper
const boughtCandyPrices = [];
const amountToSpend = Math.random() * 100;

function addCandy(candyType, weight) {
  if (candyType === "sweet") {
    boughtCnadyPrices.push(0.5 * weight);
  }
}
